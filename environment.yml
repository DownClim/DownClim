# environment.yml — conda environment definition
# The authoritative list is in pyproject.toml under [tool.conda].
name: downclim
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  - llvmlite
  - numba
  - dask
  - netCDF4
  - xesmf
  # Everything else is installed via pip (the package + deps + extras)
  - pip:
      - -e ".[test,dev,docs]"
